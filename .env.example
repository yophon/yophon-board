HOST=127.0.0.1
PORT=3020
DB_PATH=data/yophon-board.db
DEFAULT_BOARD_SLUG=main
DEFAULT_BOARD_TITLE=Yophon Board
BOARD_ADMIN_PASSWORD=change-me
# APP_ORIGIN=https://board.example.com
# Force secure cookies. Leave unset to auto-detect via X-Forwarded-Proto;
# set to true behind HTTPS reverse proxies, false only for local plain HTTP.
# COOKIE_SECURE=true
